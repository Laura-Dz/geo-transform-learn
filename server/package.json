{
  "name": "geo-transform-learn-server",
  "version": "1.0.0",
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.14.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/cors": "^2.8.13",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "prisma": {
    "schema": "../prisma/schema.prisma"
  },
    "scripts": {
      "dev": "cd .. && npx prisma generate && cd server && ts-node --transpile-only api/index.ts",
      "build": "cd .. && npx prisma generate && cd server && tsc",
      "start": "node build/index.js",
      "prisma:generate": "cd .. && npx prisma generate",
      "prisma:push": "cd .. && npx prisma db push"
    }
}
